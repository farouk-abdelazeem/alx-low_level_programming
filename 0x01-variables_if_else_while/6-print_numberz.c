#include <stdio.h>
/**
 * main - prints the alphabet in lowercase.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char digit = '1';

while (digit < '10')
{
putchar(digit);
digit++;
}
putchar('\n');

	return (0);
}
