#include <stdio.h>
/**
 * main - prints the alphabet in lowercase.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int digit = 0;

for (; digit < 10 ; digit++)
{
putchar('0' + digit);
}
putchar('\n');
	return (0);
}
